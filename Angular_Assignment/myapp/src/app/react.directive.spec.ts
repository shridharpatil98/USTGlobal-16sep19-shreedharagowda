import { ReactDirective } from './react.directive';

describe('ReactDirective', () => {
  it('should create an instance', () => {
    const directive = new ReactDirective();
    expect(directive).toBeTruthy();
  });
});
